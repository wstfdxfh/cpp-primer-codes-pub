#include <string>
#include <iostream>
#include <cctype>

using std::string;
using std::cin;
using std::cout;
using std::endl;
using std::cerr;
using std::getline;
using std::ispunct;
using std::toupper;

int main(int argc, char* argv[]) {
  int temp;
  // 当遇到文件结束或者输入为42时候退出循环
  while (cin >> temp && temp != 42) {
  }

  return 0;
}